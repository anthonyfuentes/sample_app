
<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
            remote: true) do |form_builder| %>
  <%= form_builder.submit "Unfollow", class: "btn" %>
<% end %>
