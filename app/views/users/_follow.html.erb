
<%= form_for(current_user.active_relationships.build, remote: true) do |form_build| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= form_build.submit "Follow", class: "btn btn-primary" %>
<% end %>
